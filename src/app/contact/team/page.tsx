import {Menu} from "@/common/components/Menu";

export const metadata = {
    title: "Team next.js app",
    description: "ths is team page",
}

export default  function TeamPage () {
    return (
        <div>
            <Menu />
            <h1>Team Page</h1>
        </div>
    );
}